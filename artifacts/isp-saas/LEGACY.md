# LEGACY / REFERENCE — not the production backend

This Django tree was an earlier foundation experiment.

**Do not deploy it. Do not add ISP business logic here.**

The live product is the TanStack Start app at the repository root (`src/`).

Kept for:
- permission/role modeling ideas (adapted into `src/lib/isp/rbac.ts`)
- historical ADRs under `artifacts/isp-saas/docs/`
