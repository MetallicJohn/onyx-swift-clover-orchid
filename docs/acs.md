# ACS / GenieACS

GenieACS is a **separate service**. This app stores CPE inventory (`cpe_devices`) and will hold an ACS adapter (server URL, tenant mapping, provisioning jobs).

The current ACS screen lists seeded CPE rows. That is **not** GenieACS integration.

**Status:** inventory UI **partial**; GenieACS adapter **architecture only**.
