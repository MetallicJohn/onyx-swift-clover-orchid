# CR-021 Build as we publish to the VPS

`update.sh` pulls `origin/main` and rebuilds Docker. A systemd timer does that every few minutes. CI can SSH the same updater after a green `main` build when `VPS_HOST` + `VPS_SSH_KEY` are set. `gridline.env` is never overwritten. Health reports the git SHA baked into the image.
