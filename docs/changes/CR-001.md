# CR-001 M0.5 Architecture consolidation

- **Requirement:** Harden multi-tenant ISP SaaS without a rewrite
- **Reason:** Competing backends, first-tenant LIMIT 1, runtime DDL, fake WG keys, frontend STK confirm
- **Modules:** tenant-context, rbac, secrets, payments, ledger, wireguard, agent, docs
- **Database:** migrations/0006_hardening.sql
- **API:** `/api/v1/health`; existing webhooks unchanged
- **Breaking:** live simulated STK (`ws_*`) can no longer be confirmed unless the provider is sandbox
- **Tests:** rbac, secrets, wireguard, payments-hardening, tenant-isolation
- **Rollback:** revert 0006 (additive); restore previous `enrollFields` only if needed
- **Deploy:** run migrations, set `APP_SECRET` in production
