# CR-003 PPPoE/static access service + command approval

- **Requirement:** PPPoE/static go through RADIUS; destructive RouterOS needs approve
- **Modules:** access-service, ipam, command-policy, agent, mikrotik, services UI, routers UI
- **Database:** 0008_ipam.sql
- **API:** rotateServiceSecret, disconnectService, approveRouterCommand
- **Breaking:** `raw.script` / reboot sit in `proposed` until approved (agent will not pull them)
- **Tests:** command-policy.test.ts
