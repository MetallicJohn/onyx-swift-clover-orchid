# CR-023 Tenant UI typeface

`--font-sans` / `--default-font-family` are runtime tokens, same mechanism as colour. Each ISP picks from a curated registry (`src/lib/theme/fonts.ts`). Free-form CSS and uploaded font files are rejected. Mono (`font-mono`) and invoice PDFs stay IBM Plex Mono / Helvetica.
