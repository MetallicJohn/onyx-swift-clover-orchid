# CR-030 Remaining ISP Solutions copy

Operator-facing leftovers from the previous product id now say ISP Solutions: screenshot metadata, migration comments, and test fixtures.

Compatibility detection is unchanged so existing boxes keep working:

- `/opt/gridline`, `gridline.env`, and Docker volume `gridline_pgdata` still migrate on update
- `GRIDLINE_*` environment keys still copy into `ISPSOLUTIONS_*`
- MikroTik enroll still renames `wg-gridline` (and old script/scheduler names) to ISP Solutions ids
