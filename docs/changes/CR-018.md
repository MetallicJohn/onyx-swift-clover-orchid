# CR-018 GenieACS beside Gridline

Mongo + GenieACS containers on the VPS compose file. Gridline NBI adapter dispatches reboot/SSID/refresh and syncs CPE inventory. NBI is not published on the host. CWMP 7547 and FS 7567 are. UI on `acs.<domain>`.
