# CR-048 Routers desk load time

The Routers page listed the fleet only after tenant seed re-provisioned every live service and four extra server calls finished. Opening the page also waited on the first router's detail, the agent queue, and RouterOS API credentials.

| Surface | Behaviour |
|---|---|
| Tenant seed | `seedOpsForTenant` still fills payment rails, WireGuard hub, and missing enroll tokens. It no longer emits `service.changed` for every service, so viewing Routers does not queue a MikroTik command per line. Real create/change/payment still calls `provisionServiceAccess`. |
| List | One `listRouters` call returns the fleet, IP pools, hub readiness, and bootstrap domain. The table paints as soon as that returns. |
| Detail | First-router detail loads in the background. Agent queue and RouterOS API load only after **Show agent queue and RouterOS API**. Empty state says **Loading routers…** until the list arrives, not **No routers yet**. |
| Not changed | Bootstrap `check-certificate=yes`, domain resolver, provisioning tokens, CR-039–047. |

Proof: `access.test.ts`.
