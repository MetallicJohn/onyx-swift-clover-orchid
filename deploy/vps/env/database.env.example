# Database VPS — PostgreSQL only.
POSTGRES_USER=gridline
POSTGRES_DB=gridline
POSTGRES_PASSWORD=change-me
# Private / VPN address of this VPS. Default 127.0.0.1 refuses remote clients.
POSTGRES_BIND=10.200.0.20
